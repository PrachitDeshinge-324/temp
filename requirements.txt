# Core deep learning and computer vision
torch
opencv-python
numpy
pyyaml

# YOLOv8 (Ultralytics)
ultralytics

# EfficientDet (using efficientdet-pytorch)
# efficientdet-pytorch

# ByteTrack and BoT-SORT dependencies (use open-source implementations)
scipy
filterpy

# For visualization (optional)
matplotlib

# For OSNet and other CNN-based ReID models
torchreid
# For transformer-based ReID (TransReID), install manually from the official repo:
# See: https://github.com/microsoft/TransReID
# Example:
git clone https://github.com/damo-cv/TransReID.git
cd TransReID
pip install -r requirements.txt

